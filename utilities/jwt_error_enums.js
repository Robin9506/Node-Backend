const Jwt_error_enums = {
    JSON_WEB_TOKEN_ERROR: "Token is Invalid",
    TOKEN_EXPIRED: "Token Expired",
    NOT_BEFORE_ERROR: "Token is before the current time"
}

module.exports = Jwt_error_enums;

